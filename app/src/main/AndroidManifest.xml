<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gita.holybooks.bhagwatgita"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.HomePageActivity"
            android:label="@string/activity_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ChapterSlidePagerActivity"
            android:label="@string/activity_chapters"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.SettingsActivity" />
        <activity
            android:name=".activity.ShlokaOfTheDayActivity"
            android:label="@string/app_name" />

        <service
            android:name=".service.NotificationService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activity.ShareActivity" />
        <activity android:name=".activity.RateAppActivity" />
        <activity android:name=".activity.BookmarkActivity" />
        <activity android:name=".activity.NoteActivity" />
        <activity android:name=".activity.ProgressReportActivity" />
        <activity
            android:name=".activity.ChapterActivity"
            android:label="@string/title_activity_chapter"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>